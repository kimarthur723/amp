# amp

The goal of this project is to build a lightweight guitar amp simulator,
with a framework for creating effects that can be added to the signal chain.

## Features

Currently, the project only supports audio passthrough.

## Build

Build using `make`. Run `make clean` to clean up compiled executables.
